<% if flash.any? %>
  <% user_facing_flashed.each do ⎪key, value⎪ %>
    <div class="alert alert-dismissable alert-<%= flash_class(key) %>">
       <button class="close" data-sidmiss="alert">×</button>
       <%= value %>
    </div>
  <% end %>
<% end %>
